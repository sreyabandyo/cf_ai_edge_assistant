# AI Edge Assistant (Cloudflare Workers)

This is project is an AI-powered backend built using Cloudflare Workers and Workers AI.

## Features
- Accepts POST requests with a message
- Uses Llama 3 model to generate responses
- Runs on Cloudflare edge network

## How to run locally 
```bash
wrangler dev 